#include <stdio.h>
#include <stdlib.h>
/**
 * main - Entry point
 *
 * Return: 0
 *
 */
int main(void)
{
	cha low, e, q;

	e = 'e'
	q = 'q'

	for (low = 'a'; low <= 'z'; low++)
	{
	if (low != e  && low != q)
	putchar(low);
	}
	putchar('\n');
	return (0);
}
